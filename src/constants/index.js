import * as ROUTES from './routes';

const ERRORS = {
  'auth/email-already-in-use': 'Email já em uso por outra conta',
  'auth/user-not-found': 'Email não encontrado',
  'auth/wrong-password': 'Senha incorreta',
};

export { ERRORS, ROUTES };
